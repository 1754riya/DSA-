class Solution {
  public:
    int largest(vector<int> &arr) {
        int x=arr.size();
        int maxvalue=arr[0];
       for(int i=1;i<x;i++){
           if(arr[i]>maxvalue){
               maxvalue=arr[i];
           }
       }
       return maxvalue;
        
    }
};
